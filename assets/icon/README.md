Place your app launcher icon image here as app_icon.png. This should be a square PNG (at least 1024x1024 for best results).

After adding the image, run:
- flutter pub get
- flutter pub run flutter_launcher_icons:main

This will update Android and iOS launcher icons.
